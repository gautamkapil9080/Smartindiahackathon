import React from 'react';
import { Typography } from '@mui/material';

const Profile = () => {
  return (
    <Typography variant="h4">Profile - Coming Soon</Typography>
  );
};

export default Profile;
