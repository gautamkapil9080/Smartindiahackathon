import React from 'react';
import { Typography } from '@mui/material';

const Register = () => {
  return (
    <Typography variant="h4">Register - Coming Soon</Typography>
  );
};

export default Register;